export interface Config {
  reference: string;
  size: number;
  defaultValue: string;
  dataType: string;
  name: string;
  maximum: number;
  description: string;
  id: number;
  minimum: number;
  nullValue: string;
  labels: string;
}
